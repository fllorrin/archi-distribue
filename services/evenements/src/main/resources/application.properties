spring.application.name=evenements
server.port=${SERVER_PORT:8084}

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5436/evenements}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:evenements}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:evenements}

spring.jpa.hibernate.ddl-auto=validate

spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=0

management.otlp.metrics.export.enabled=${OTLP_METRICS_EXPORT_ENABLED:false}
management.tracing.sampling.probability=${OTLP_TRACING_SAMPLING:1.0}
logging.structured.format.console=logstash

spring.graphql.path=/graphql

app.security.enabled=${APP_SECURITY_ENABLED:true}
spring.security.oauth2.resourceserver.jwt.issuer-uri=${JWT_ISSUER_URI:http://keycloak:8080/realms/LoCarE}

app.kafka.enabled=${KAFKA_ENABLED:true}
app.kafka.topic.vehicle=${KAFKA_TOPIC_VEHICLE:vehicule_topic}
app.kafka.group-id=${KAFKA_GROUP_ID:evenements-service}

spring.kafka.bootstrap-servers=${SPRING_KAFKA_BOOTSTRAP_SERVERS:localhost:29092}
spring.kafka.consumer.group-id=${app.kafka.group-id}
spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.springframework.kafka.support.serializer.JsonDeserializer
spring.kafka.consumer.properties.spring.json.trusted.packages=fr.univrouen.evenements.event.messaging
spring.kafka.consumer.properties.spring.json.value.default.type=fr.univrouen.evenements.event.messaging.VehicleEvent
spring.kafka.consumer.properties.spring.json.use.type.headers=false
